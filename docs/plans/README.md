# Plans

Planes de implementación (Spec-Driven Development), generados por el pipeline `/sdd`.

- Naming: `<slug>.md` (kebab-case en español), mismo slug que la spec.
- Convención: cada feature tiene su plan aquí y su spec en `docs/specs/`.
- Template completo en `.opencode/agents/architect.md`.
