# Specs

Specs de features (Spec-Driven Development), generadas por el pipeline `/sdd`.

- Naming: `<slug>.md` (kebab-case en español).
- Convención: cada feature tiene su spec aquí y su plan en `docs/plans/`.
- Template completo en `.opencode/agents/spec-writer.md`.
